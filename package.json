{
  "name": "interview-flashcard-quiz-platform",
  "version": "1.0.0",
  "description": "Flashcard Quiz Platform with Spaced Repetition, Analytics, and Dark Mode",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:seed": "npm run prisma:seed --prefix backend",
    "setup": "npm run install:all && npm run prisma:migrate && npm run prisma:seed"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
